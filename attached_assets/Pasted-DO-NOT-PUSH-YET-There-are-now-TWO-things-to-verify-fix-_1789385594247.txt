DO NOT PUSH YET.

There are now TWO things to verify/fix:

1. Current HEAD is:
2776cf8bf0a67d00040525959213e0e3a254c06e

and e48c5f4 is NOT an ancestor.

Do NOT reset to e48c5f4.

Instead verify whether the CURRENT code still contains the same verified fixes/features from e48c5f4:

- automatic scheduler every 900 seconds
- startup scan
- v!scan does NOT reset automatic next-scan deadline
- config/job_sources.json fallback works
- 26 sources load
- web job UI remains disabled
- Discord Bot is primary
- persistent control panel
- dynamic Discord timestamps
- Discord-only interactions

If current HEAD already has equivalent/newer implementations, KEEP current HEAD.

2. FIX:
"AFTER HOURS JOB HUNTER didn’t respond in time"

Every Discord button/modal interaction MUST acknowledge within 3 seconds.

For every potentially slow interaction:
- immediately defer/respond
- THEN perform work asynchronously
- use followup/edit_original_response when complete

Examples:
SCAN NOW
SEARCH JOBS
STATUS
LATEST JOBS
APPLY NOW
SAVE
SKIP
cover-letter generation
resume preview

Do NOT wait for:
- Bright Data
- ATS scanning
- database-heavy work
- Gemini
- PDF generation
before acknowledging Discord.

For long work:

interaction
→ immediate defer/ACK
→ background async task
→ edit/followup result

Also inspect for synchronous/blocking work inside the Discord event loop.

Move blocking calls such as synchronous HTTP, file/PDF work, or heavy pipeline functions to:
- proper async clients
or
- asyncio.to_thread(...)

Do NOT block Discord heartbeat while scanning.

Persistent component callbacks must also:
- have stable custom_id values
- persistent View timeout=None where required
- be restored on bot startup

TEST REAL DISCORD INTERACTIONS:

SCAN NOW
SEARCH JOBS
STATUS
LATEST JOBS
HELP
SAVE
SKIP
APPLY NOW

NONE may show:
"application did not respond"
or
"didn’t respond in time"

Then verify:

- Discord bot healthy during a full scan
- heartbeat is not blocked
- scheduler still 900 seconds
- source JSON still loads 26 sources
- next automatic scan remains correct after v!scan
- Discord-only UI remains intact

Run tests.

ONLY AFTER ALL OF THIS PASSES:

1. commit the fixes
2. push current HEAD to:
https://github.com/draiimon/job-scraper
branch main

Do NOT push private resume or secrets.

FINAL RESPONSE ONLY:

CURRENT HEAD: [hash]
OLD e48c5f4 FEATURES PRESERVED: YES/NO
DISCORD INSTANT ACK: PASS/FAIL
NO INTERACTION TIMEOUT: PASS/FAIL
EVENT LOOP NON-BLOCKING: PASS/FAIL
BOT HEARTBEAT: PASS/FAIL
SCHEDULER: PASS/FAIL
AUTO INTERVAL: [seconds]
v!scan PRESERVES NEXT AUTO SCAN: PASS/FAIL
SOURCE JSON: PASS/FAIL
SOURCES LOADED: [number]
DISCORD-ONLY UI: PASS/FAIL
CONTROL PANEL: PASS/FAIL
DYNAMIC TIME: PASS/FAIL
TESTS: X passed / X failed
COMMIT: [hash]
PUSH: PASS/BLOCKED
BLOCKER: [only if any]