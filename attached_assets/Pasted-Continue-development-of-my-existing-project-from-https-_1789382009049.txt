Continue development of my existing project from:

https://github.com/draiimon/job-scraper

IMPORTANT:
This repository belongs to GitHub user `draiimon`.
Never use, configure, push to, or authenticate as any other GitHub user.

Clone/pull the latest `main` branch and INSPECT THE EXISTING CODE FIRST.

This is an EXISTING WORKING PROJECT.
Do NOT rebuild it from scratch.

PURPOSE:
After Hours Job Hunter is my Philippine entry-level tech job monitoring system.

Current architecture includes:
- FastAPI
- Supabase PostgreSQL
- automatic scheduler every 15 minutes
- 26+ public ATS/company sources
- Bright Data LinkedIn job discovery
- optional Bright Data JobStreet integration
- Discord Bot as PRIMARY Discord UI
- Discord webhook as emergency fallback only
- Gemini for optional cover-letter revision
- resume + cover-letter application workflow
- Docker / Render deployment support

CURRENT DISCORD DESIGN:

Discord BOT is primary.

Commands:
- v!search <role>
- v!latest
- v!status
- v!scan
- v!help

Control-panel interactions:
- SCAN NOW
- SEARCH JOBS
- LATEST JOBS
- STATUS
- HELP

Job actions:
- VIEW JOB
- APPLY NOW
- SAVE
- SKIP

Internal actions must stay inside Discord.

Only VIEW JOB / actual employer application portal may open an external webpage.

CURRENT SCANNER BEHAVIOR:

Automatic scan:
every 15 minutes

Manual v!scan:
- acknowledge immediately
- run asynchronously in background
- one global scan at a time
- 5-minute cooldown
- progress/status updates
- concurrent ATS processing
- timeout slow sources safely

v!search:
- targeted search
- recent cache/database first
- Bright Data LinkedIn when needed
- recent ACTIVE jobs only
- Philippines / Remote PH
- entry-level tech prioritization
- deduplicate
- return Discord job cards

FRESHNESS:
0–24h highest priority
1–3 days high
4–7 days normal
8–14 days lower
15–30 days exceptional matches only
30+ days normally skip

Do not treat "newly discovered" as "newly posted."

FILTERING:
Preferred:
DevOps, Cloud, Infrastructure, Systems, Linux, Platform, SRE

Also notify:
IT Support, Technical Support, Service Desk, NOC, Networking, Software Development, QA, Cybersecurity, Application Support, IT Operations and other legitimate entry-level computer/IT roles.

Reject unrelated:
Accounting, HR, Finance, Sales, Marketing, generic VA/Admin, nontechnical customer service, etc.

AI:
Gemini must NEVER be required for normal job monitoring.

Gemini is primarily used when APPLY NOW is selected:
truthful deterministic cover-letter draft
→ optional Gemini revision
→ factual validation
→ application review

If Gemini fails/rate-limits:
use deterministic cover-letter fallback.

DISCORD:
Keep polished dark/orange embeds.

Footer:
After Hours Job Hunter • Made by masoncalix

Remove:
- svg artifacts
- image artifacts
- ugly raw Markdown
- excessive emojis
- internal escaped identifiers

SECRETS:
Use Replit Secrets/environment variables ONLY.

Never hard-code or print:
- DISCORD_BOT_TOKEN
- DISCORD_WEBHOOK_URL
- DATABASE_URL
- GEMINI_API_KEY(S)
- BRIGHTDATA_API_TOKEN
- Google OAuth credentials
- APP_SECRET_KEY

Do NOT commit `.env`.

REPLIT:
Configure this project so it can run and be tested safely inside Replit.

Do NOT redesign architecture specifically for Replit.

Preserve Render compatibility because Render remains my intended production host.

Do NOT introduce Replit-specific dependencies that break Docker/Render.

FIRST TASKS:

1. Pull latest main.
2. Inspect repository completely.
3. Read current README, .env.example, requirements.txt, Dockerfile and src/.
4. Run existing tests.
5. Identify unfinished work/TODOs/current failures.
6. Verify the Discord bot starts correctly.
7. Verify v!search, v!scan, v!latest, v!status and v!help.
8. Verify automatic 15-minute scheduler remains active.
9. Verify Supabase connection.
10. Verify Bright Data LinkedIn.
11. Verify cover-letter/application workflow.
12. Fix incomplete functionality without rewriting working code.
13. Keep webhook fallback only.
14. Run all tests after changes.
15. Push completed changes ONLY to:
    https://github.com/draiimon/job-scraper
    branch: main

Do NOT push until tests pass.

Also replace/update README.md so it accurately documents the CURRENT implementation.

Keep responses concise to save Replit Agent usage.

Start by inspecting the repository and report only:

CURRENT STATE:
UNFINISHED:
TESTS:
SECRETS NEEDED:
NEXT:

Then proceed with implementation.